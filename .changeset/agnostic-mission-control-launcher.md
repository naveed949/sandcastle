---
"@ai-hero/sandcastle": minor
---

Add durable, independently controllable repository workflows to Mission
Control, with a launcher that executes the same planner, parallel implementer,
reviewer, feature-branch integration, and issue-closing flow as `run.ts`.
