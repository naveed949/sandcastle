---
"@ai-hero/sandcastle": minor
---

Add an independent reviewer stage with a bounded remediation loop for verified
implementation work: schema-validated versioned verdicts with findings,
locations, rationale, and required actions; a deterministic approval gate that
rejects blocking findings on approved verdicts; read-only review that receives
the frozen task, accepted plan, implementation diff, verification evidence,
and repository policy as text; append-only remediation iterations with
explicit retry identities; configurable iteration and time budgets that enter
manual intervention when exhausted; and Mission Control projection of every
review in the workflow projection.
