---
"@ai-hero/sandcastle": patch
---

Add Cursor CLI (`agent`) support via the `cursorAgent()` factory: stream-json parsing, init scaffolding and Dockerfile template, and Claude-only session file validation when using `resumeSession`.
